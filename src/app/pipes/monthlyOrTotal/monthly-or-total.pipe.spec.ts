import { MonthlyOrTotalPipe } from './monthly-or-total.pipe';

describe('MonthlyOrTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthlyOrTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
